/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode

# --- storage 内の item フォルダは Git に追加したい ---
# まず storage 以下を無視
/storage/*

# ただし item フォルダ以下は除外（Git に追加）
!/storage/app/public/item
!/storage/app/public/item/**

# 上記の設定により、storage/app/public/item フォルダと中の画像だけ Git に含める

# MySQLのデータディレクトリはGit管理しない
docker/mysql/data/

# src の外の docker/mysql/data は ../docker/mysql/data/ ではなく絶対パスで指定
/docker/mysql/data/